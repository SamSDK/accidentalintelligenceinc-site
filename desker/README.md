# Desker — hosted here at /desker

This is a **deployed copy**, synced from the canonical source at `C:\Users\Sam\Claude\Projects\Desker-Site\` (repo `SamSDK/desker-site`, still live at `samsdk.github.io/desker-site` pending a decision on that URL).

**Make future content/copy/pricing edits in the source repo, not here** — this folder only exists so the site renders at `accidentalintelligenceinc.com/desker`. Canonical URL, Open Graph, and JSON-LD tags in this copy's `index.html` point to `accidentalintelligenceinc.com/desker/` (updated 2026-08-19); the source repo's copy still points to the old `samsdk.github.io` URL.

Re-sync after a source edit:
```bash
cp ../../Desker-Site/{index.html,favicon.ico,favicon.svg,logo.png,logo-16.png,og.png} .
# then re-apply the URL substitution: samsdk.github.io/desker-site/ -> accidentalintelligenceinc.com/desker/
```
